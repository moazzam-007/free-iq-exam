# ==============================================================================
# MASTER PROMPT: PHASE 1 — CORE FLAGSHIP TESTS & SCORING FLOW (4 PAGES)
# Target Files:
# 1. src/pages/test.astro (20-min 24-item 2PL IRT Assessment Engine)
# 2. src/pages/quick-test.astro (10-min 12-item Rapid Screening)
# 3. src/pages/results.astro (Instant 2PL IRT Score Report & Bell Curve)
# 4. src/pages/practice.astro (Untimed Adaptive Practice Hub & Explanations)
# ==============================================================================

You are an expert Frontend Design Engineer specializing in Astro 5+, Tailwind CSS v4, WCAG AAA accessibility, psychometric assessment UI, and Emil Kowalski interaction physics.

### MISSION:
Refactor and elevate the 4 Core Flagship Assessment & Scoring Flow pages in `freeiqexam-ui-enhanced-v2` (`src/pages/test.astro`, `src/pages/quick-test.astro`, `src/pages/results.astro`, `src/pages/practice.astro`) to 100% align with our Master Design System, Option 1 Typography Triad, 1-Color Action Blue palette, and anti-AI-slop standards.

---

### STRICT ARCHITECTURAL & DESIGN SYSTEM RULES (NON-NEGOTIABLE):

1. **Option 1 Typography Triad (Strictly Applied)**:
   - **Display & Headings**: `font-family: 'Geist', -apple-system, sans-serif;` with tight tracking (`-0.025em` on H1, `-0.02em` on H2s).
   - **Body Copy**: `font-family: 'Inter', -apple-system, sans-serif;` locked to Apple 17px standard (`text-[17px] sm:text-lg`, 1.62 leading).
   - **Badges, Timers, Telemetry, Percentiles & Keys**: `font-family: 'Geist Mono', 'JetBrains Mono', monospace;` with a strict $\ge 13\text{px}$ floor (`text-[13px] sm:text-sm`).

2. **1-Color Action Blue + Monochrome Duet**:
   - **Action Blue**: `#0066cc` (Light) / `#2997ff` (Dark) with `#0071e3` / `#3b82f6` hover states.
   - **Surfaces**: `#ffffff` / `#f5f5f7` (Light) vs `#000000` / `#090a0f` / `#0f1016` (Dark).
   - **Hairlines**: `1px solid var(--fx-line)` (`#e4e4e7` Light, `rgba(255,255,255,0.08)` Dark).
   - **FORBIDDEN**: Remove ALL purple/indigo gradients (e.g. `from-blue-600 to-indigo-600`, `from-blue-500/10 via-indigo-500/10 to-purple-500/10`). Use only crisp monochrome surfaces and Action Blue accents.
   - **Feedback states**: Emerald (`#10b981`) for correct / Rose (`#f43f5e`) for error / Amber (`#f59e0b`) for timeout warnings.

3. **Candidate 2 Luxury Ambient Radial Glow**:
   - Apply optical depth behind the assessment cards and hero score report:
     `--fx-hero-glow-light: radial-gradient(circle at 50% 10%, rgba(0, 102, 204, 0.10) 0%, rgba(0, 102, 204, 0.03) 45%, transparent 70%);`
     `--fx-hero-glow-dark: radial-gradient(circle at 50% 10%, rgba(41, 151, 255, 0.12) 0%, rgba(0, 102, 204, 0.04) 45%, transparent 70%);`
   - Guarded with `display: none` under `prefers-reduced-motion`.

4. **Emil Kowalski Tactile Physics**:
   - Buttons, keypad options, prev/next arrows, and tab pills: `active:scale(0.97)` with `160ms cubic-bezier(0.23, 1, 0.32, 1)`.
   - Option card hover: `translateY(-2px)` with 1px border brightening and subtle ambient halo.
   - All interactive touch targets $\ge 44\text{px}$.

---

### DETAILED SPECIFICATIONS PER PAGE:

#### 1. `src/pages/test.astro` & `src/pages/quick-test.astro` (Full 20-min & Quick 10-min Engines)
- **Top Header Bar**:
  - Exit button: Clean monochrome brand icon (remove indigo gradient).
  - Timer: `Geist Mono` countdown pill with live pulse indicator.
  - Linear Progress Bar: Seamless 2px Action Blue progress track.
  - Submit Button: Pill shape with `active:scale(0.97)` and `#0066cc` fill.
- **Assessment Arena**:
  - Domain Badge: `Geist Mono` neutral badge (e.g., `Gf · Fluid Reasoning`, `Gv · Visual-Spatial`) with $\ge 13\text{px}$ floor.
  - Question Instruction: 17px/18px clean `Inter` text.
  - Stimulus Card: High-contrast, clean container with `1px solid var(--fx-line)`, no nested double borders.
  - Options Grid: 6 response option tiles with shortcut number badges (`1` to `6`), hover `translateY(-2px)`, selected state with Action Blue outline (`box-shadow: 0 0 0 2px var(--fx-action)`), min-height $\ge 52\text{px}$.
  - Navigation Bar: Sticky bottom bar with `Previous` and `Next / Skip` buttons with Emil tactile press.
- **Logic Integrity**:
  - Keep 100% of the existing JavaScript scoring engine, 2PL IRT ability estimation, answer key token decoding, storage fallbacks, and screen reader announcements (`#sr-announcer`).

#### 2. `src/pages/results.astro` (Instant 2PL IRT Score & Bell Curve Report)
- **Monumental Score Showcase**:
  - Monumental `Geist` number (e.g. `IQ 128`), percentile pill (`97th Percentile · Top 3%`), and 95% Posterior Interval ($\theta \pm 1.96 \cdot \text{SE}$).
  - Candidate 2 ambient glow background wash (remove multi-color blur).
- **Gaussian Bell Curve Distribution**:
  - Interactive / canvas Gaussian normal distribution curve (mean 100, SD 15) with standard deviation bands ($\pm 1\sigma, \pm 2\sigma, \pm 3\sigma$) and user score indicator pin.
- **4-Domain Cognitive Breakdown Bento**:
  - 4 clean cards for Fluid Reasoning (Gf), Visual-Spatial (Gv), Quantitative Logic (Gq), and Verbal Comprehension (Gc).
  - Each card features: Domain badge, raw score, percentile bar, and clinical construct interpretation.
- **Action Docks & Print Support**:
  - "Save PDF / Print Official Report" (`window.print()`), "Retake Assessment", and "Practice Weak Areas" buttons.
  - Clean `@media print` rules for watermark-free clinician-ready PDF printout.

#### 3. `src/pages/practice.astro` (Untimed Adaptive Practice Hub & Explanations)
- **Domain Filter Tabs**:
  - Segmented pill tabs (`All Domains`, `Fluid (Gf)`, `Visual-Spatial (Gv)`, `Quantitative (Gq)`, `Verbal (Gc)`).
- **Interactive Solution Explanations**:
  - Item family explanation cards explaining Carpenter matrix transformation rules (Progression, XOR Cancellation, Topological Net Folding, Distribution of Three).
  - Interactive "Show Solution & Transformation Rule" accordion toggle with instant tactile feedback.

---

### VERIFICATION & ZERO-ERROR ACCEPTANCE CRITERIA:
- [ ] Option 1 typography (`Geist` / `Inter` / `Geist Mono` $\ge 13\text{px}$) applied across all 4 files.
- [ ] 0 purple/indigo gradients, 0 AI-slop decorative rainbows.
- [ ] All touch targets $\ge 44\text{px}$ and tactile `active:scale(0.97)` applied to all buttons.
- [ ] Full scoring logic, timer engine, answer key token, and print styles preserved with 100% fidelity.
- [ ] Run `npm run build` and ensure all 171 static pages build with 0 errors.
