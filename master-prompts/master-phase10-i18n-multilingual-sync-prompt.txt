# ==============================================================================
# MASTER PROMPT: PHASE 10 — MULTI-LANGUAGE I18N SYNCHRONIZATION (RU & ID LOCALES)
# Target Directories:
# 1. src/pages/ru/ (Russian Locale Pages — 26 root pages + 10 game subpages)
# 2. src/pages/id/ (Indonesian Locale Pages — 26 root pages + 10 game subpages)
# ==============================================================================

You are an expert Frontend Design Engineer specializing in Astro 5+ i18n Architecture, Cyrillic / Latin Typography Tuning, RTL/LTR Localization Standards, WCAG AAA accessibility, and Emil Kowalski interaction physics.

### MISSION:
Synchronize and elevate all localized pages in `src/pages/ru/` and `src/pages/id/` of `freeiqexam-ui-enhanced-v2` to 100% reflect the Master Design System, Option 1 Typography Triad, 1-Color Action Blue palette, Candidate 2 ambient glow, and anti-AI-slop design standards established in Phases 0–9, while preserving 100% of all translated strings, localized route slugs, and metadata schemas.

---

### STRICT ARCHITECTURAL & I18N RULES (NON-NEGOTIABLE):

1. **Option 1 Typography Triad for International Locales**:
   - **Headings (Cyrillic & Indonesian)**: `font-family: 'Geist', -apple-system, sans-serif;` with tight tracking (`-0.02em` on Cyrillic/Indonesian H1/H2).
   - **Body Standard**: `font-family: 'Inter', -apple-system, sans-serif;` locked to Apple 17px standard (`text-[17px] sm:text-lg`, 1.62 leading).
   - **Badges, Timers, Math Symbols, Telemetry, Keypad Shortcuts**: `font-family: 'Geist Mono', 'JetBrains Mono', monospace;` with a strict >= 13px floor (`text-[13px] sm:text-sm`).
   - **Zero Translation Alterations**: Do NOT retranslate, rewrite, or alter Russian / Indonesian vocabulary, idioms, clinical descriptions, or localized FAQ strings.

2. **1-Color Action Blue + Monochrome Duet**:
   - **Action Blue**: `#0066cc` (Light) / `#2997ff` (Dark) with `#0071e3` / `#3b82f6` hover states.
   - **Surfaces**: `#ffffff` / `#f5f5f7` (Light) vs `#000000` / `#090a0f` / `#0f1016` (Dark).
   - **Hairlines**: `1px solid var(--fx-line)` (`#e4e4e7` Light, `rgba(255,255,255,0.08)` Dark).
   - **FORBIDDEN**: Remove ALL rainbow gradient text, pastel tag badges (`pastel-tag-*`), and purple/indigo buttons across `/ru/` and `/id/`.

3. **Candidate 2 Luxury Ambient Radial Glow**:
   - Apply scoped optical depth behind testing arenas, games, calculators, and hero report cards across all `/ru/` and `/id/` pages:
     `--fx-hero-glow-light: radial-gradient(circle at 50% 10%, rgba(0, 102, 204, 0.10) 0%, rgba(0, 102, 204, 0.03) 45%, transparent 70%);`
     `--fx-hero-glow-dark: radial-gradient(circle at 50% 10%, rgba(41, 151, 255, 0.12) 0%, rgba(0, 102, 204, 0.04) 45%, transparent 70%);`
   - Guarded with `display: none` under `prefers-reduced-motion`.

4. **Emil Kowalski Tactile Physics**:
   - Option cards, answer tiles, language switchers, drill buttons, and calculator controls: `active:scale(0.97)` with `160ms cubic-bezier(0.23, 1, 0.32, 1)`.
   - Touch targets strictly >= 44px on all mobile viewports.

5. **Local Slug & Component Path Integrity**:
   - Keep all relative layout/component imports pointing accurately (`../../layouts/Layout.astro`, `../../components/Header.astro`, `../../components/Footer.astro`).
   - Preserve localized route paths (e.g., `/id/tes`, `/id/hasil`, `/id/permainan/*`, `/ru/games/*`).
   - Preserve `currentPath` and locale props passed to Header/Footer.

---

### VERIFICATION & ZERO-ERROR ACCEPTANCE CRITERIA:
- [ ] Option 1 typography (`Geist` / `Inter` / `Geist Mono` >= 13px) applied across all `/ru/` and `/id/` files.
- [ ] 0 pastel tags, 0 AI-slop multicolored gradients, 0 sub-13px typography.
- [ ] 100% of Russian & Indonesian copy, translations, schemas, and scoring logic intact.
- [ ] All touch targets >= 44px with `active:scale(0.97)` tactile response.
- [ ] Run `npm run build` and ensure all 171 static pages build with 0 errors.
