# ==============================================================================
# MASTER PROMPT: PHASE 5 — VISUOMOTOR REFLEX & SPEED BENCHMARKS (4 PAGES)
# Target Files:
# 1. src/pages/reaction-time-test.astro (Visual Reflex Latency Engine)
# 2. src/pages/typing-test.astro (WPM Benchmark & Keyboard Error Heatmap)
# 3. src/pages/aim-trainer.astro (60 FPS Precision Flick & Smooth Tracking)
# 4. src/pages/flag-quiz.astro (195+ World Flag Visual Recall Challenge)
# ==============================================================================

You are an expert Frontend Design Engineer specializing in Astro 5+, Tailwind CSS v4, High-Performance 60FPS Canvas, Web Audio API, and Emil Kowalski interaction physics.

### MISSION:
Refactor and elevate the 4 Visuomotor Reflex & Speed Benchmark pages in `freeiqexam-ui-enhanced-v2` (`src/pages/reaction-time-test.astro`, `src/pages/typing-test.astro`, `src/pages/aim-trainer.astro`, `src/pages/flag-quiz.astro`) to 100% align with our Master Design System, Option 1 Typography Triad, 1-Color Action Blue palette, Candidate 2 ambient glow, and anti-AI-slop design standards.

---

### STRICT ARCHITECTURAL & DESIGN SYSTEM RULES (NON-NEGOTIABLE):

1. **Option 1 Typography Triad (Strictly Enforced)**:
   - **Headings & H1/H2**: `font-family: 'Geist', -apple-system, sans-serif;` with tight tracking (`-0.025em` on H1, `-0.02em` on H2s).
   - **Body Standard**: `font-family: 'Inter', -apple-system, sans-serif;` locked to Apple 17px standard (`text-[17px] sm:text-lg`, 1.62 leading).
   - **Badges, Milliseconds (ms), WPM, Accuracy (%), Score, FPS, Telemetry**: `font-family: 'Geist Mono', 'JetBrains Mono', monospace;` with a strict >= 13px floor (`text-[13px] sm:text-sm`).

2. **1-Color Action Blue + Monochrome Duet**:
   - **Action Blue**: `#0066cc` (Light) / `#2997ff` (Dark) with `#0071e3` / `#3b82f6` hover states.
   - **Surfaces**: `#ffffff` / `#f5f5f7` (Light) vs `#000000` / `#090a0f` / `#0f1016` (Dark).
   - **Hairlines**: `1px solid var(--fx-line)` (`#e4e4e7` Light, `rgba(255,255,255,0.08)` Dark).
   - **FORBIDDEN**: Remove ALL purple/indigo gradients (e.g. `from-blue-600 to-indigo-600`). Use clean monochrome surfaces with Action Blue controls.
   - **Game/Reflex State Colors**: Waiting state (Amber/Zinc) / Click/Green state (`#10b981`) / Early False-Start state (`#f43f5e`) / Typing Error state (`#f43f5e`).

3. **Candidate 2 Luxury Ambient Radial Glow**:
   - Apply optical depth behind the reflex and arena playfields:
     `--fx-hero-glow-light: radial-gradient(circle at 50% 10%, rgba(0, 102, 204, 0.10) 0%, rgba(0, 102, 204, 0.03) 45%, transparent 70%);`
     `--fx-hero-glow-dark: radial-gradient(circle at 50% 10%, rgba(41, 151, 255, 0.12) 0%, rgba(0, 102, 204, 0.04) 45%, transparent 70%);`
   - Guarded with `display: none` under `prefers-reduced-motion`.

4. **Emil Kowalski Tactile Physics**:
   - Mode tabs ("Gridshot" vs "Tracking", "Burst 15s" vs "60s", Continent filters), start buttons, and control pads: `active:scale(0.97)` with `160ms cubic-bezier(0.23, 1, 0.32, 1)`.
   - Control card hover: `translateY(-2px)` with 1px border brightening and ambient halo.
   - All interactive touch targets >= 44px.

---

### DETAILED SPECIFICATIONS PER PAGE:

#### 1. `src/pages/reaction-time-test.astro` (Visual Reflex Latency Engine):
- **Hero & Badge**: `MILLISECOND LATENCY · HARDWARE-ACCELERATED` in clean monochrome pill.
- **Reaction Arena**:
  - Full-screen or centered reflex target area with instant color switches (Red: Wait, Green: Click Now!, Blue: Results summary).
  - High-precision `performance.now()` millisecond readout in `Geist Mono` (`text-5xl sm:text-6xl font-bold`).
- **Trial History & Distribution**:
  - 5-trial breakdown table with individual latency stamps, mean calculation, outlier rejection, and percentile comparison curve.

#### 2. `src/pages/typing-test.astro` (WPM Speed & Accuracy Benchmark):
- **Hero & Badge**: `WPM BENCHMARK · MONKEYTYPE-GRADE ACCURACY`.
- **Typing Arena**:
  - Monkeytype-style smooth caret, word stream container with 1px hairline border.
  - Duration Pill Tabs (`15s`, `30s`, `60s`, `120s`) with tactile `active:scale(0.97)`.
  - Mechanical switch sound toggle (Cherry MX Blue, Brown, Red audio synthesis).
- **Interactive Keyboard Error Heatmap**:
  - QWERTY SVG/Canvas keyboard layout displaying finger accuracy and per-key mistake heatmaps.

#### 3. `src/pages/aim-trainer.astro` (FPS Precision Aim Benchmark):
- **Hero & Badge**: `60 FPS TRACKING · ESPORTS TELEMETRY`.
- **Canvas Playfield**:
  - High-performance 60FPS+ HTML5 Canvas arena with sub-pixel hitboxes and particle splash.
  - Mode selector pills: `Gridshot (Flick)`, `Smooth Tracking`, `Precision Micro`.
  - eDPI sensitivity converter selector (Valorant, CS2, Overwatch 2, Apex Legends).
- **Esports Scorecard**:
  - Real-time kinematic metrics: Score, Targets Per Second (TPS), Raw Accuracy %, and Median Flick Delay in `Geist Mono`.

#### 4. `src/pages/flag-quiz.astro` (195+ World Flag Visual Recall):
- **Hero & Badge**: `195+ SOVEREIGN NATIONS · GEOGRAPHIC MEMORY`.
- **Gameplay Deck & Filter Bar**:
  - Continental Filter Capsule: `All World`, `Europe`, `Asia`, `Americas`, `Africa`, `Oceania`.
  - Mode Pills: `20-Round Sprint`, `Survival`, `Reverse Guessing`, `Hard Mode`.
  - 4-choice response buttons with 44px touch targets, `active:scale(0.97)`, and instant green/red accuracy feedback.
  - Streak multiplier counter and regional mastery progress meter.

---

### VERIFICATION & ZERO-ERROR ACCEPTANCE CRITERIA:
- [ ] Option 1 typography (`Geist` / `Inter` / `Geist Mono` >= 13px) applied to all 4 files.
- [ ] 0 purple/indigo gradients, 0 AI-slop decorative colors.
- [ ] All high-precision timing, 60FPS animation loops, keyboard listeners, and flag vector graphics preserved with 100% fidelity.
- [ ] All touch targets >= 44px with `active:scale(0.97)` tactile response.
- [ ] Run `npm run build` and ensure all 171 static pages build with 0 errors.
