---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const title = "Dual N-Back Memory - Working Memory Training | FreeIQExam";
const description = "Scientific Dual N-Back cognitive drill. Simultaneously track spatial positions and visual letters to expand working memory capacity and attention control.";
---

<Layout title={title} description={description}>
  <Header />

  <main class="flex-1 py-10 md:py-14 px-4 sm:px-6 lg:px-8 max-w-4xl mx-auto w-full">
    <!-- Breadcrumb -->
    <nav class="flex items-center text-xs font-mono text-zinc-500 dark:text-zinc-400 mb-6" aria-label="Breadcrumb">
      <a href="/" class="hover:text-zinc-900 dark:hover:text-white transition-colors">Home</a>
      <span class="mx-2">/</span>
      <a href="/games" class="hover:text-zinc-900 dark:hover:text-white transition-colors">Cognitive Games</a>
      <span class="mx-2">/</span>
      <span class="text-zinc-900 dark:text-zinc-100 font-semibold">Dual N-Back</span>
    </nav>

    <!-- Header / HUD -->
    <section class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 pb-6 mb-6 border-b border-zinc-200 dark:border-zinc-800">
      <div>
        <div class="inline-flex items-center gap-2 px-2.5 py-0.5 rounded-full text-xs font-semibold bg-blue-50 text-blue-700 dark:bg-blue-950/60 dark:text-blue-300 border border-blue-200 dark:border-blue-800 mb-1.5">
          Gwm - Working Memory
        </div>
        <h1 class="text-2xl sm:text-3xl font-heading font-bold text-zinc-950 dark:text-white">
          Dual N-Back Memory
        </h1>
      </div>

      <!-- Stat HUD -->
      <div class="flex items-center gap-3 font-mono text-xs sm:text-sm">
        <div class="px-3 py-1.5 rounded-xl bg-zinc-100 dark:bg-zinc-800/90 border border-zinc-200 dark:border-zinc-700 text-center">
          <div class="text-zinc-500 dark:text-zinc-400 uppercase text-xs font-mono font-medium tracking-wider">Level</div>
          <div id="hud-n-level" class="font-bold text-blue-600 dark:text-blue-400 text-base">2-Back</div>
        </div>
        <div class="px-3 py-1.5 rounded-xl bg-zinc-100 dark:bg-zinc-800/90 border border-zinc-200 dark:border-zinc-700 text-center">
          <div class="text-zinc-500 dark:text-zinc-400 uppercase text-xs font-mono font-medium tracking-wider">Trial</div>
          <div id="hud-trial-count" class="font-bold text-zinc-800 dark:text-zinc-200 text-base">0 / 20</div>
        </div>
        <div class="px-3 py-1.5 rounded-xl bg-zinc-100 dark:bg-zinc-800/90 border border-zinc-200 dark:border-zinc-700 text-center">
          <div class="text-zinc-500 dark:text-zinc-400 uppercase text-xs font-mono font-medium tracking-wider">Score</div>
          <div id="hud-score" class="font-bold text-emerald-600 dark:text-emerald-400 text-base">0</div>
        </div>
        <div class="px-3 py-1.5 rounded-xl bg-zinc-100 dark:bg-zinc-800/90 border border-zinc-200 dark:border-zinc-700 text-center">
          <div class="text-zinc-500 dark:text-zinc-400 uppercase text-xs font-mono font-medium tracking-wider">Best</div>
          <div id="hud-best-score" class="font-bold text-zinc-700 dark:text-zinc-300 text-base">--</div>
        </div>
      </div>
    </section>

    <!-- Main Game Arena -->
    <div class="relative bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-3xl p-6 sm:p-10 shadow-sm flex flex-col items-center">
      <!-- Start Screen Overlay -->
      <div id="start-overlay" class="absolute inset-0 z-20 bg-white/95 dark:bg-zinc-900/95 backdrop-blur-md rounded-3xl flex flex-col items-center justify-center p-6 text-center">
        <div class="w-16 h-16 rounded-2xl bg-blue-600 dark:bg-blue-500 text-white flex items-center justify-center mb-5 shadow-lg shadow-blue-500/20">
          <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 4.5v15m6-15v15m-10.5-7.5h15" />
          </svg>
        </div>
        <h2 class="text-2xl sm:text-3xl font-heading font-extrabold text-zinc-950 dark:text-white mb-3">
          Dual N-Back Challenge
        </h2>
        <div class="max-w-md text-sm text-zinc-600 dark:text-zinc-300 leading-relaxed mb-6 space-y-2">
          <p>
            Observe the highlighted <strong>grid position</strong> and the simultaneous <strong>letter</strong> on each trial.
          </p>
          <p>
            Press <strong>Position Match (A)</strong> when the position matches <span id="overlay-n-text" class="font-bold text-blue-600 dark:text-blue-400">2 steps back</span>.
          </p>
          <p>
            Press <strong>Letter Match (L)</strong> when the letter matches <span class="font-bold text-indigo-600 dark:text-indigo-400">2 steps back</span>.
          </p>
          <p class="text-xs text-zinc-500 dark:text-zinc-400 pt-2 font-mono">
            Scoring: +10 pts per correct match, -5 pts for false alarms.
          </p>
        </div>
        <button
          id="btn-start-game"
          type="button"
          class="min-h-[44px] px-8 py-3.5 rounded-2xl bg-blue-600 hover:bg-blue-500 text-white font-heading font-bold text-base shadow-md transition-all duration-150 hover:scale-105 active:scale-95 flex items-center gap-2 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-600"
        >
          <span>Start Session (Space)</span>
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5.25 5.653c0-.856.917-1.398 1.667-.986l11.54 6.348a1.125 1.125 0 010 1.971l-11.54 6.347a1.125 1.125 0 01-1.667-.985V5.653z" />
          </svg>
        </button>
      </div>

      <!-- Stimulus Area & Response (inert when overlay active) -->
      <div id="game-arena-content" class="w-full flex flex-col items-center">
        <!-- Stimulus Area: Grid + Letter Card -->
        <div class="w-full max-w-sm flex flex-col items-center gap-6 mb-8">
          <!-- Letter Card Display -->
          <div class="w-24 h-24 sm:w-28 sm:h-28 rounded-2xl bg-zinc-100 dark:bg-zinc-800/80 border-2 border-zinc-200 dark:border-zinc-700/80 flex flex-col items-center justify-center shadow-inner relative transition-all">
            <span class="text-xs font-mono uppercase tracking-widest text-zinc-500 dark:text-zinc-400 absolute top-2">Letter</span>
            <span id="stimulus-letter" class="font-heading font-extrabold text-4xl sm:text-5xl text-zinc-950 dark:text-white transition-opacity duration-150">-</span>
          </div>

          <!-- 3x3 Spatial Grid -->
          <div class="grid grid-cols-3 gap-3 p-3 rounded-2xl bg-zinc-100 dark:bg-zinc-800/50 border border-zinc-200 dark:border-zinc-800 shadow-inner w-full aspect-square max-w-[280px]">
            {Array.from({ length: 9 }).map((_, i) => (
              <div
                id={`cell-${i}`}
                class="rounded-xl border border-zinc-200 dark:border-zinc-700/60 bg-white dark:bg-zinc-900 transition-all duration-200 flex items-center justify-center"
              >
                <div class="w-2.5 h-2.5 rounded-full bg-zinc-300 dark:bg-zinc-700"></div>
              </div>
            ))}
          </div>
        </div>

        <!-- Action Response Buttons -->
        <div class="w-full max-w-md grid grid-cols-2 gap-4">
          <button
            id="btn-pos-match"
            type="button"
            class="min-h-[56px] py-4 px-4 rounded-2xl bg-blue-50 dark:bg-blue-950/40 border-2 border-blue-500 text-blue-700 dark:text-blue-300 font-heading font-bold text-sm sm:text-base transition-all duration-100 hover:bg-blue-100 dark:hover:bg-blue-900/50 active:scale-95 flex flex-col items-center justify-center shadow-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-600"
          >
            <span>Position Match</span>
            <span class="text-xs font-mono font-normal opacity-75 mt-0.5">Key [ A ]</span>
          </button>

          <button
            id="btn-letter-match"
            type="button"
            class="min-h-[56px] py-4 px-4 rounded-2xl bg-indigo-50 dark:bg-indigo-950/40 border-2 border-indigo-500 text-indigo-700 dark:text-indigo-300 font-heading font-bold text-sm sm:text-base transition-all duration-100 hover:bg-indigo-100 dark:hover:bg-indigo-900/50 active:scale-95 flex flex-col items-center justify-center shadow-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-600"
          >
            <span>Letter Match</span>
            <span class="text-xs font-mono font-normal opacity-75 mt-0.5">Key [ L ]</span>
          </button>
        </div>

        <!-- Feedback Toast -->
        <div id="feedback-toast" role="status" aria-live="polite" class="h-6 mt-4 text-xs font-mono font-semibold transition-opacity opacity-0 text-zinc-500">
          Input received
        </div>
      </div>
    </div>

    <!-- End of Round Modal -->
    <div id="modal-end-round" role="dialog" aria-modal="true" aria-labelledby="modal-title" tabindex="-1" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm hidden items-center justify-center p-4">
      <div class="bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-3xl p-6 sm:p-8 max-w-md w-full shadow-2xl text-center">
        <div id="modal-icon-container" class="w-14 h-14 rounded-2xl bg-emerald-100 text-emerald-600 dark:bg-emerald-950/60 dark:text-emerald-400 flex items-center justify-center mx-auto mb-4">
          <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>

        <h3 id="modal-title" class="text-2xl font-heading font-bold text-zinc-950 dark:text-white mb-1">
          Round Completed!
        </h3>
        <p id="modal-level-status" class="text-sm font-medium text-blue-600 dark:text-blue-400 mb-6">
          Level Evaluation
        </p>

        <!-- Stats Breakdown -->
        <div class="grid grid-cols-3 gap-3 mb-6 font-mono text-center">
          <div class="p-3 rounded-xl bg-zinc-50 dark:bg-zinc-800/60 border border-zinc-200 dark:border-zinc-700/60">
            <div class="text-zinc-400 text-[10px] uppercase">Final Score</div>
            <div id="modal-score" class="font-bold text-lg text-zinc-900 dark:text-white">0</div>
          </div>
          <div class="p-3 rounded-xl bg-zinc-50 dark:bg-zinc-800/60 border border-zinc-200 dark:border-zinc-700/60">
            <div class="text-zinc-400 text-[10px] uppercase">Accuracy</div>
            <div id="modal-accuracy" class="font-bold text-lg text-emerald-600 dark:text-emerald-400">0%</div>
          </div>
          <div class="p-3 rounded-xl bg-zinc-50 dark:bg-zinc-800/60 border border-zinc-200 dark:border-zinc-700/60">
            <div class="text-zinc-400 text-[10px] uppercase">Next Level</div>
            <div id="modal-next-n" class="font-bold text-lg text-indigo-600 dark:text-indigo-400">2-Back</div>
          </div>
        </div>

        <div id="modal-best-alert" class="hidden mb-6 p-2.5 rounded-xl bg-amber-50 dark:bg-amber-950/40 border border-amber-200 dark:border-amber-800 text-amber-800 dark:text-amber-300 text-xs font-semibold">
          New Personal Best Record!
        </div>

        <div class="flex items-center gap-3">
          <button
            id="btn-modal-next-round"
            type="button"
            class="flex-1 py-3 rounded-xl bg-blue-600 hover:bg-blue-500 text-white font-semibold text-sm shadow-sm transition-all duration-150 hover:scale-102 active:scale-98"
          >
            Play Next Round
          </button>
          <a
            href="/games"
            class="px-5 py-3 rounded-xl bg-zinc-100 hover:bg-zinc-200 dark:bg-zinc-800 dark:hover:bg-zinc-700 text-zinc-700 dark:text-zinc-200 font-semibold text-sm transition-colors"
          >
            Hub
          </a>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</Layout>

<script is:inline>
  (function() {
    var TOTAL_TRIALS = 20;
    var STIMULUS_DURATION = 500;
    var TRIAL_INTERVAL = 2500;
    var LETTERS = ['C', 'H', 'K', 'L', 'Q', 'R', 'S', 'T'];

    var currentN = 2;
    var score = 0;
    var trialIndex = 0;
    var trials = [];
    var isRunning = false;
    var trialTimer = null;
    var clearStimulusTimer = null;

    // User input registered for current trial
    var pressedPos = false;
    var pressedLetter = false;

    // Session stats
    var correctPosMatches = 0;
    var correctLetterMatches = 0;
    var falsePosMatches = 0;
    var falseLetterMatches = 0;
    var totalDecisions = 0;
    var correctDecisions = 0;

    // DOM references
    var hudNLevel = document.getElementById('hud-n-level');
    var hudTrialCount = document.getElementById('hud-trial-count');
    var hudScore = document.getElementById('hud-score');
    var hudBestScore = document.getElementById('hud-best-score');
    var startOverlay = document.getElementById('start-overlay');
    var overlayNText = document.getElementById('overlay-n-text');
    var btnStartGame = document.getElementById('btn-start-game');
    var stimulusLetter = document.getElementById('stimulus-letter');
    var btnPosMatch = document.getElementById('btn-pos-match');
    var btnLetterMatch = document.getElementById('btn-letter-match');
    var gameArenaContent = document.getElementById('game-arena-content');
    var feedbackToast = document.getElementById('feedback-toast');
    var modalEndRound = document.getElementById('modal-end-round');
    var modalScore = document.getElementById('modal-score');
    var modalAccuracy = document.getElementById('modal-accuracy');
    var modalNextN = document.getElementById('modal-next-n');
    var modalLevelStatus = document.getElementById('modal-level-status');
    var modalBestAlert = document.getElementById('modal-best-alert');
    var btnModalNextRound = document.getElementById('btn-modal-next-round');

    // Initial accessibility state: inert while start overlay is active
    if (gameArenaContent) gameArenaContent.setAttribute('inert', '');

    function loadBestScore() {
      try {
        var saved = localStorage.getItem('fiq_game_nback_best');
        if (saved) {
          hudBestScore.textContent = saved;
        }
      } catch (e) {}
    }
    loadBestScore();

    function updateHud() {
      hudNLevel.textContent = currentN + '-Back';
      hudTrialCount.textContent = trialIndex + ' / ' + TOTAL_TRIALS;
      hudScore.textContent = score;
      if (overlayNText) {
        overlayNText.textContent = currentN + ' steps back';
      }
    }

    function showFeedback(text, isPositive) {
      if (!feedbackToast) return;
      var icon = isPositive ? '✓ ' : '✗ ';
      feedbackToast.textContent = icon + text;
      feedbackToast.className = 'h-6 mt-4 text-xs font-mono font-semibold transition-opacity ' +
        (isPositive ? 'text-emerald-600 dark:text-emerald-400' : 'text-rose-600 dark:text-rose-400');
      feedbackToast.style.opacity = '1';
      setTimeout(function() {
        feedbackToast.style.opacity = '0';
      }, 700);
    }

    function generateTrialSequence() {
      trials = [];
      for (var i = 0; i < TOTAL_TRIALS; i++) {
        var pos;
        var letter;

        // Position decision
        if (i >= currentN && Math.random() < 0.35) {
          pos = trials[i - currentN].pos;
        } else {
          var availPos = [0, 1, 2, 3, 4, 5, 6, 7, 8];
          if (i >= currentN) {
            availPos = availPos.filter(function(p) { return p !== trials[i - currentN].pos; });
          }
          pos = availPos[Math.floor(Math.random() * availPos.length)];
        }

        // Letter decision
        if (i >= currentN && Math.random() < 0.35) {
          letter = trials[i - currentN].letter;
        } else {
          var availLetters = LETTERS.slice();
          if (i >= currentN) {
            availLetters = availLetters.filter(function(l) { return l !== trials[i - currentN].letter; });
          }
          letter = availLetters[Math.floor(Math.random() * availLetters.length)];
        }

        trials.push({ pos: pos, letter: letter });
      }
    }

    function highlightCell(idx, active) {
      var cell = document.getElementById('cell-' + idx);
      if (!cell) return;
      if (active) {
        cell.className = 'rounded-xl border-2 border-blue-500 bg-blue-500 shadow-lg shadow-blue-500/50 flex items-center justify-center scale-105 transition-all duration-150';
        cell.innerHTML = '<div class=\"w-3.5 h-3.5 rounded-full bg-white animate-pulse\"></div>';
      } else {
        cell.className = 'rounded-xl border border-zinc-200 dark:border-zinc-700/60 bg-white dark:bg-zinc-900 transition-all duration-200 flex items-center justify-center';
        cell.innerHTML = '<div class=\"w-2.5 h-2.5 rounded-full bg-zinc-300 dark:bg-zinc-700\"></div>';
      }
    }

    function recordPositionInput() {
      if (!isRunning || pressedPos) return;
      pressedPos = true;
      btnPosMatch.classList.add('ring-4', 'ring-blue-400', 'bg-blue-600', 'text-white');

      if (trialIndex > currentN) {
        var current = trials[trialIndex - 1];
        var past = trials[trialIndex - 1 - currentN];
        var isMatch = current.pos === past.pos;
        if (isMatch) {
          score += 10;
          correctPosMatches++;
          showFeedback('+10 Position Match!', true);
        } else {
          score = Math.max(0, score - 5);
          falsePosMatches++;
          showFeedback('-5 False Alarm!', false);
        }
        hudScore.textContent = score;
      }
    }

    function recordLetterInput() {
      if (!isRunning || pressedLetter) return;
      pressedLetter = true;
      btnLetterMatch.classList.add('ring-4', 'ring-indigo-400', 'bg-indigo-600', 'text-white');

      if (trialIndex > currentN) {
        var current = trials[trialIndex - 1];
        var past = trials[trialIndex - 1 - currentN];
        var isMatch = current.letter === past.letter;
        if (isMatch) {
          score += 10;
          correctLetterMatches++;
          showFeedback('+10 Letter Match!', true);
        } else {
          score = Math.max(0, score - 5);
          falseLetterMatches++;
          showFeedback('-5 False Alarm!', false);
        }
        hudScore.textContent = score;
      }
    }

    function evaluateMisses(evaluatedIndex) {
      if (evaluatedIndex <= currentN) return;
      var cur = trials[evaluatedIndex - 1];
      var past = trials[evaluatedIndex - 1 - currentN];

      var posMatched = cur.pos === past.pos;
      var letterMatched = cur.letter === past.letter;

      // Opportunities count
      totalDecisions += 2;

      // Position evaluation
      if (posMatched && pressedPos) {
        correctDecisions++;
      } else if (!posMatched && !pressedPos) {
        correctDecisions++;
      }

      // Letter evaluation
      if (letterMatched && pressedLetter) {
        correctDecisions++;
      } else if (!letterMatched && !pressedLetter) {
        correctDecisions++;
      }
    }

    function resetInputButtons() {
      pressedPos = false;
      pressedLetter = false;
      btnPosMatch.className = 'py-4 px-4 rounded-2xl bg-blue-50 dark:bg-blue-950/40 border-2 border-blue-500 text-blue-700 dark:text-blue-300 font-heading font-bold text-sm sm:text-base transition-all duration-100 hover:bg-blue-100 dark:hover:bg-blue-900/50 active:scale-95 flex flex-col items-center justify-center shadow-sm';
      btnLetterMatch.className = 'py-4 px-4 rounded-2xl bg-indigo-50 dark:bg-indigo-950/40 border-2 border-indigo-500 text-indigo-700 dark:text-indigo-300 font-heading font-bold text-sm sm:text-base transition-all duration-100 hover:bg-indigo-100 dark:hover:bg-indigo-900/50 active:scale-95 flex flex-col items-center justify-center shadow-sm';
    }

    function runNextTrial() {
      if (trialIndex > 0) {
        evaluateMisses(trialIndex);
      }

      if (trialIndex >= TOTAL_TRIALS) {
        finishRound();
        return;
      }

      resetInputButtons();
      var t = trials[trialIndex];
      trialIndex++;
      updateHud();

      // Show stimulus
      highlightCell(t.pos, true);
      stimulusLetter.textContent = t.letter;
      stimulusLetter.style.opacity = '1';

      // Hide stimulus after STIMULUS_DURATION
      clearStimulusTimer = setTimeout(function() {
        highlightCell(t.pos, false);
        stimulusLetter.textContent = '-';
        stimulusLetter.style.opacity = '0.3';
      }, STIMULUS_DURATION);

      trialTimer = setTimeout(runNextTrial, TRIAL_INTERVAL);
    }

    function startRound() {
      if (isRunning) return;
      isRunning = true;
      trialIndex = 0;
      score = 0;
      correctPosMatches = 0;
      correctLetterMatches = 0;
      falsePosMatches = 0;
      falseLetterMatches = 0;
      totalDecisions = 0;
      correctDecisions = 0;

      generateTrialSequence();
      updateHud();

      startOverlay.classList.add('hidden');
      modalEndRound.classList.add('hidden');
      modalEndRound.classList.remove('flex');
      if (gameArenaContent) gameArenaContent.removeAttribute('inert');

      runNextTrial();
    }

    function finishRound() {
      isRunning = false;
      clearTimeout(trialTimer);
      clearTimeout(clearStimulusTimer);
      resetInputButtons();
      if (gameArenaContent) gameArenaContent.setAttribute('inert', '');

      var accuracy = totalDecisions > 0 ? Math.round((correctDecisions / totalDecisions) * 100) : 0;

      var prevN = currentN;
      var statusMsg = '';
      if (accuracy >= 80) {
        currentN++;
        statusMsg = 'Promoted! Advanced to ' + currentN + '-Back based on ' + accuracy + '% accuracy.';
      } else if (accuracy < 60) {
        currentN = Math.max(1, currentN - 1);
        statusMsg = 'Regressed to ' + currentN + '-Back to consolidate working memory baseline.';
      } else {
        statusMsg = 'Maintained ' + currentN + '-Back. Target 80%+ accuracy to level up.';
      }

      var savedBest = 0;
      try {
        savedBest = parseInt(localStorage.getItem('fiq_game_nback_best') || '0', 10);
      } catch (e) {}

      var isNewBest = score > savedBest;
      if (isNewBest) {
        try {
          localStorage.setItem('fiq_game_nback_best', score.toString());
        } catch (e) {}
        loadBestScore();
      }

      modalScore.textContent = score;
      modalAccuracy.textContent = accuracy + '%';
      modalNextN.textContent = currentN + '-Back';
      modalLevelStatus.textContent = statusMsg;

      if (isNewBest && score > 0) {
        modalBestAlert.classList.remove('hidden');
      } else {
        modalBestAlert.classList.add('hidden');
      }

      modalEndRound.classList.remove('hidden');
      modalEndRound.classList.add('flex');
      modalEndRound.focus();
    }

    // Button event listeners
    btnStartGame.addEventListener('click', startRound);
    btnPosMatch.addEventListener('click', recordPositionInput);
    btnLetterMatch.addEventListener('click', recordLetterInput);
    btnModalNextRound.addEventListener('click', function() {
      modalEndRound.classList.add('hidden');
      modalEndRound.classList.remove('flex');
      startRound();
    });

    // Keyboard shortcuts
    window.addEventListener('keydown', function(e) {
      if (e.repeat) return;
      var key = e.key.toLowerCase();
      if (key === ' ' && !isRunning && modalEndRound.classList.contains('hidden')) {
        e.preventDefault();
        startRound();
      } else if (key === 'escape' && !modalEndRound.classList.contains('hidden')) {
        modalEndRound.classList.add('hidden');
        modalEndRound.classList.remove('flex');
        startOverlay.classList.remove('hidden');
        if (gameArenaContent) gameArenaContent.setAttribute('inert', '');
      } else if (key === 'a') {
        recordPositionInput();
      } else if (key === 'l') {
        recordLetterInput();
      }
    });
  })();
</script>
